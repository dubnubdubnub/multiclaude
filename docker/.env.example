# Copy this to .env and fill in your values.
# Do NOT commit .env to version control.

# Required: your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: gh CLI token (auto-extracted by run.sh if gh is installed)
# GH_TOKEN=gho_...
